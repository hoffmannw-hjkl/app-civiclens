apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: civiclens

resources:
  - namespace.yaml
  - backend-config.yaml
  - managed-cert.yaml
  - service-account.yaml
  - service.yaml
  - deployment.yaml
  - ingress.yaml
