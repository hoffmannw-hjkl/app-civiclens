apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: civiclens-pdb
  namespace: civiclens
  labels:
    app: civiclens-api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: civiclens-api
